# Choose experiments with the run/skip blocks in src/main.py.
show_plots: true
experiments:
  offline_temporal:
    always:
      threshold: 50.0
      interval: [0, 1]
    eventually:
      threshold: 55.0
      interval: [0, 1]
