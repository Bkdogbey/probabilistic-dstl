# Optimizer defaults; a scenario's `planner:` block overrides them.
w_phi: 1.0
w_u: 1.0e-3
w_du: 1.0e-3
lr: 0.05
max_iters: 300
smoothing:
  beta_start: 10.0
  beta_end: 200.0
alpha: null      # stop once exact lower robustness reaches it; null runs all
