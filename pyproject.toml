[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "probabilistic-dstl"
version = "0.1.0"
description = "Probability-bound differentiable Signal Temporal Logic planning"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = [
  "matplotlib",
  "numpy",
  "Pillow",
  "PyYAML",
  "scipy",
  "torch",
]

[project.optional-dependencies]
dev = ["build", "flake8", "pytest", "ruff"]

[tool.ruff]
line-length = 79

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["utils"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["experiments*", "models*", "pdstl*", "planning*", "visualization*"]

[tool.setuptools.data-files]
"share/probabilistic-dstl/configs" = ["configs/*.yaml"]
"share/probabilistic-dstl/configs/scenarios/lane" = [
  "configs/scenarios/lane/*.yaml",
]
"share/probabilistic-dstl/configs/scenarios/reach_avoid" = [
  "configs/scenarios/reach_avoid/*.yaml",
]

[tool.pytest.ini_options]
markers = [
    "slow: full scenario solves (minutes); deselect with -m 'not slow'",
]
